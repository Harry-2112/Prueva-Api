const { response } = require("express");

function name(params) {
	return id;
}

function fetch() {
	fetch("zerxtrctvybunon.com")
		.then((data) => data.json())
		.then((response) => console.log(response));
}
